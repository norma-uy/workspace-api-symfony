framework:
    assets:
        packages:
            'admin-config':
                json_manifest_path: '%kernel.project_dir%/public/build/admin/manifest.json'
            'docs-config':
                json_manifest_path: '%kernel.project_dir%/public/build/docs/manifest.json'

#when@prod:
#    webpack_encore:
#        # Cache the entrypoints.json (rebuild Symfony's cache when entrypoints.json changes)
#        # Available in version 1.2
#        cache: true

#when@test:
#    webpack_encore:
#        strict_mode: false
